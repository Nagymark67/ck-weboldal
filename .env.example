# Example environment variables for local development
# Copy this file to .env and edit values as needed (if using dotenv or other loader).

# Server port
PORT=3000

# Database (XAMPP default MySQL settings typically use root with no password)
DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=
DB_NAME=users

# CORS
# Comma-separated list of allowed origins (no spaces)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# Or allow all origins (useful for ngrok during development)
# CORS_ALLOW_ALL=1
